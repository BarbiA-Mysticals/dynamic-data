{{!-- This is a helper to evaluate if the variable or expression exits --}}
{{#if data.image }}
    <img class="hero img-fluid" src="{{data.image}}" alt="">
{{/if}}
<h1 class="primary-heading">{{data.title}}</h1>
<p class="abstract txtBody">{{data.abstract}}</p>